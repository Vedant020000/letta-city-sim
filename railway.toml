[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.bundle"

[deploy]
preDeployCommand = ["/app/scripts/db-bootstrap.sh"]
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
